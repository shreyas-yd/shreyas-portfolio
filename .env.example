# Contentful (blog) — create a space at contentful.com and a CDA token.
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=
# Optional: preview token + environment
CONTENTFUL_PREVIEW_ACCESS_TOKEN=
CONTENTFUL_ENVIRONMENT=master

# Analytics (already hard-coded to the existing site's IDs in components/Analytics.tsx,
# but exposed here if you want to make them configurable)
NEXT_PUBLIC_GA_ID=G-NE06LFKPMT
NEXT_PUBLIC_CLARITY_ID=vym36u4i45
